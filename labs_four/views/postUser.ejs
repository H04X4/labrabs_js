<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Добавить пользователя</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body class="p-4">
    <h1>Добавить пользователя</h1>
    <form action="/postUser" method="POST">
        <div class="mb-3">
            <label>Имя</label>
            <input type="text" name="firstName" class="form-control" required>
        </div>
        <div class="mb-3">
            <label>Фамилия</label>
            <input type="text" name="lastName" class="form-control" required>
        </div>
        <div class="mb-3">
            <label>Рейтинг</label>
            <input type="number" name="rating" class="form-control" required>
        </div>
        <div class="mb-3">
            <label>Пол</label><br>
            <input type="radio" name="gender" value="0" required> Женский
            <input type="radio" name="gender" value="1"> Мужской
        </div>
        <div class="mb-3">
            <label>Направления</label><br>
            <% arrayDirects.forEach((dir, index) => { %>
                <input type="checkbox" name="arrayDirects" value="<%= index %>"> <%= dir %><br>
            <% }) %>
        </div>
        <button type="submit" class="btn btn-success">Добавить</button>
    </form>
</body>
</html>